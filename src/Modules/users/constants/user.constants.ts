// ✅ Define once — reuse everywhere
export const USER_SELECT =
  'firstName lastName fullName email role age address profilePicture permissions active createdAt';

export const USER_QUERY_OPTIONS = {
  select: USER_SELECT,
};
