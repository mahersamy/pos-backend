// ✅ Define once — reuse everywhere
export const USER_SELECT =
  'firstName lastName fullName email role age address profilePicture.secure_url permissions active createdAt';

export const USER_QUERY_OPTIONS = {
  select: USER_SELECT,
};
